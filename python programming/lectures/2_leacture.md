## Overview: Programming Lab Instruction and Python Fundamentals

Education session covering GitHub submission workflow and foundational Python concepts

One instructor and multiple students; individual student identities are not consistently clear

Critical outcomes:

Practical work must be uploaded to GitHub regularly

Students should organize work into separate practical or lab folders

Core Python topics included type casting, errors, variables, expressions, strings, indexing, slicing, and string methods

## GitHub Workflow for Practical Work

GitHub repositories will contain students’ completed practical work so progress and feedback remain visible

Each practical should be pushed after completion rather than waiting to submit all work together

Separate files or folders should be maintained for Practical 1, Practical 2, Practical 3, and similar activities

Lab organization can use folders such as Lab 1, Lab 2, and Lab 3

Daily coding does not need to be pushed unless it is part of the assigned practical or required work

The instructor may provide a specific update or submission date when necessary

Python practical files should be uploaded after the work is completed; unfinished or untidy files should not be submitted prematurely

## Python Type Casting and Basic Errors

**Type casting**: converting a value from one data type to another, such as converting an integer into another numeric type

Strings containing letters, such as `"ABC"`, cannot be directly converted into integers because the characters do not represent a numeric value

Numeric values can be converted when the target data type can interpret them correctly

Equality comparisons use matching values and types; strings such as `"hello"` and `"Hello"` are different because their letter cases do not match

A misspelled function name, such as writing an incorrect version of `print`, produces a `NameError`

Python is an interpreted language, so code is processed line by line and errors are identified during execution

## Variables, Expressions, and Operator Precedence

Variables store values so they can be reused in calculations and printed later

Simple expressions can be written directly when faster computation and minimal memory usage are preferred

Variables become more useful when expressions or problem statements become complicated

Arithmetic examples included addition, subtraction, and division

BODMAS/operator precedence determines the order in which arithmetic operations are evaluated

Students were asked to calculate expressions such as adding `30` and `70`, and subtracting `5` from `25` before dividing the result by `10`

Practical-lab time examples included `43` minutes, `42` minutes, and `37` minutes for different practicals

## Strings, Indexing, and Slicing

Characters and groups of characters are represented as strings in Python

Spaces count as string characters and can appear as output without causing an error

Positive indexing identifies characters from the left, while negative indexing identifies positions from the right

String slicing selects a range of characters using start and end positions

A slice such as `8:12` selects characters according to the specified index range, with the ending position excluded

Output formatting matters: string results may appear inside quotation marks, and spaces must be represented correctly

String concatenation combines two strings using the `+` operator

String multiplication repeats a string multiple times, such as repeating text six times with `* 6`

## String Modification and Search Methods

String replacement modifies one value by substituting another value in its place

The `find()` method searches for a substring and returns the position of its occurrence

When a substring appears more than once, the search may identify the first occurrence

A search fails when the requested substring does not exist in the original string

Regular expressions were deferred for later instruction because they had not yet been covered in the current lesson

## Course Structure and Learning Direction

The course progresses from Python basics toward Python for data science and AI

Planned learning areas include data science concepts, Python projects, expressions, language fundamentals, and practical lab exercises

Students may upload a Python project after completing it

Foundational topics such as data types, casting, variables, expressions, and strings are intended to support later work with functions, classes, and objects

Independent practice and reviewing course material were encouraged because not every concept can be covered fully during class

---

## Action items

**Students**: Push each completed practical or assigned practical work to GitHub

**Students**: Keep practical files organized in separate repository folders such as Lab 1, Lab 2, and Lab 3

**Students**: Practice type casting, arithmetic expressions, string indexing, slicing, concatenation, repetition, replacement, and `find()`